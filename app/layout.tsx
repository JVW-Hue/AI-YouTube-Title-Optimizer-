import './globals.css'
import { Inter } from 'next/font/google'
import { Toaster } from 'react-hot-toast'

const inter = Inter({ subsets: ['latin'] })

export const metadata = {
  title: 'AI YouTube Title Optimizer - Generate High-CTR Titles in Seconds',
  description: 'Create viral YouTube titles and thumbnail text that get more clicks. AI-powered title generator for YouTubers.',
  keywords: 'YouTube titles, AI title generator, YouTube optimization, viral titles, CTR optimization',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster 
          position="top-right"
          toastOptions={{
            duration: 4000,
            style: {
              background: '#363636',
              color: '#fff',
            },
          }}
        />
      </body>
    </html>
  )
}